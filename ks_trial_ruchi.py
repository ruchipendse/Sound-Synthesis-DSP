import numpy as np

#creating a ring buffer
ring_buff = []
x = 0
for string in range(6):
        #initialise ring buffer with zeroes
        ring_buffer.append(np.zeros(string_buffer_sizes[string], dtype=np.float32))
print(ring_buff)